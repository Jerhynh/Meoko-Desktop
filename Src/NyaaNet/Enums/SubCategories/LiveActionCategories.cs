namespace NyaaNet.Enums.SubCategories
{
    /// <summary>
    /// Represents sub-categories for the LiveAction endpoint.
    /// </summary>
    public enum LiveActionCategories
    {
        /// <summary>
        /// Routes via the /promo/ sub category.
        /// </summary>
        promo,

        /// <summary>
        /// Routes via the /eng/ sub category.
        /// </summary>
        eng,

        /// <summary>
        /// Routes via the /nonEng/ sub category.
        /// </summary>
        nonEng,

        /// <summary>
        /// Routes via the /raw/ sub category.
        /// </summary>
        raw,
    }
}
