namespace NyaaNet.Enums.SubCategories
{
    /// <summary>
    /// Represents sub-categories for the anime endpoint.
    /// </summary>
    public enum AnimeSubCategories
    {
        /// <summary>
        /// Routes via the /amv/ sub category.
        /// </summary>
        amv,

        /// <summary>
        /// Routes via the /eng/ sub category.
        /// </summary>
        eng,

        /// <summary>
        /// Routes via the /non-eng/ sub category.
        /// </summary>
        nonEng,

        /// <summary>
        /// Routes via the /raw/ sub category.
        /// </summary>
        raw,
    }
}
